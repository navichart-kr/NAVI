'use client'
/**
 * LearningOverlay — 더 이상 사용되지 않음
 *
 * 캔들 패턴 / 거래량 학습은 tutorialStore.startDynamicLesson()을 통해
 * 기존 TutorialStep 플로팅 카드 UI로 동작합니다.
 *
 * chart/page.tsx 의 import가 남아있어 빈 컴포넌트로 유지합니다.
 */
export function LearningOverlay() {
  return null
}
